<% f_method = "#{node.class.base_class.name.underscore}_ancestors(#{node.depth}i)" %>
<%= f.collection_select f_method,
                        node.self_and_siblings.where(default_params),
                        :id,
                        :name,
                        { selected: node.id, include_blank: 'Select', label: false, on: { wrapper_all: false, wrapper_input_id: true } },
                        { data: { entity_type: f.object.class.base_class.name, node_type: node.class.base_class.name, index: f.index, method: f_method, title: 'node_ancestors' } } %>
