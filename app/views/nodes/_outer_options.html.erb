<%= f.collection_select "#{node.class.base_class.name.underscore}_ancestors(#{node.depth}i)",
                        node.self_and_siblings.where(default_params),
                        :id,
                        :name,
                        { selected: node.id, include_blank: 'Select', label: false, on: { wrapper_all: false, wrapper_input_id: true } },
                        { data: { entity_type: f.object.class.base_class.name, node_type: node.class.base_class.name, index: f.index, title: 'node_ancestors' } } %>
