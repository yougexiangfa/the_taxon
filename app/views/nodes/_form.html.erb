<%= default_form_for @node do |f| %>
  <%= f.text_field :name, label: '名称：' %>
  <%= f.text_field :description, label: '描述：' %>
  <%= f.select :parent_ids, options_from_collection_for_select(@options, :id, :name, @node.parent_ids), { label: 'Parents' }, { multiple: true, class: 'ui multiple search selection dropdown' } %>
  <%= f.submit %>
<% end %>

<script>
  $('.ui.dropdown.search').dropdown({
    debug: true
  });
</script>