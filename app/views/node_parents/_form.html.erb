<%= form_for(node_parent) do |f| %>
  <% if node_parent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(node_parent.errors.count, "error") %> prohibited this node_parent from being saved:</h2>

      <ul>
      <% node_parent.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
