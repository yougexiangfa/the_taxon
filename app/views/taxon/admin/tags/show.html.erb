<div class="ui segment breadcrumb">
  <%= link_to 'Back', admin_tags_path, class: 'section' %>
  <div class="divider">/</div>
  <div class="active section">Show</div>
</div>

<table class="table is-hoverable is-fullwidth">
  <tbody>
    <tr>
      <td class="has-text-right"><%= Tag.human_attribute_name(:name) %></td>
      <td><%= @tag.name %></td>
    </tr>
    <tr>
      <td class="has-text-right"><%= Tag.human_attribute_name(:type) %></td>
      <td><%= @tag.type %></td>
    </tr>
  </tbody>
</table>
